
<fieldset>
  <%= form_for @genre do |f| %>
  <%= render 'shared/errors', object: @genre %>
    <%= f.label :name %>
    <%= f.text_field :name, size: 40, autofocus: true %>
    <p><%= f.submit %></p>
  <% end %>
</fieldset>
